{
  "name": "find-a-planner-1",
  "version": "1.0.0",
  "description": "###Installation 1. Clone the repo 2. `$ cd <path>` into the directory 3. `$ npm install` install node dependancies 4. `$ bower install` install bower dependancies 5. `$ gulp` run gulp",
  "main": "gulpfile.js",
  "dependencies": {
    "gulp": "^3.8.11",
    "gulp-angular-templatecache": "^1.6.0",
    "gulp-bower": "0.0.10",
    "gulp-gh-pages": "^0.5.2",
    "gulp-jade": "latest",
    "gulp-notify": "^2.2.0",
    "gulp-sass": "^1.3.3",
    "streamqueue": "^1.1.0"
  },
  "devDependencies": {
    "browser-sync": "^2.8.1",
    "gulp-inject": "^1.5.0"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Parker @ WF",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "null"
  }
}
